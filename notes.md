# Use OPENGL for Graphics

Inside shader program

# VAO 
which combines matrices into an array
* uses layout to bind attributes of the matrix
* using uniform to upload into shader + dont need to be respecified, stick around from object to object
* main function is setting the gl variables into the layout vars
* 